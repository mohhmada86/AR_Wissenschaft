# تفعيل التعليقات العامة — خطوة اختيارية

يعمل نموذج التعليقات فورًا دون إعداد، لكنه يحفظ التعليقات على جهاز الزائر فقط. السبب أن GitHub Pages يستضيف ملفات ثابتة ولا يوفّر قاعدة بيانات تستطيع استقبال تعليقات جميع الزوار.

إذا أردت أن تصل التعليقات إليك وأن تظهر للزوار بعد موافقتك، يمكنك ربط الموقع بمشروع Supabase مجاني. لا تضع كلمة مرور قاعدة البيانات أو مفتاح service_role أو أي مفتاح إداري داخل الموقع.

## 1. إنشاء المشروع والجدول

1. أنشئ مشروعًا في Supabase.
2. افتح **SQL Editor**.
3. الصق النص التالي كاملًا ثم شغّله مرة واحدة:

~~~sql
create table public.comments (
  id bigint generated by default as identity primary key,
  name text not null check (char_length(btrim(name)) between 2 and 60),
  comment text not null check (char_length(btrim(comment)) between 3 and 1000),
  created_at timestamptz not null default now(),
  approved boolean not null default false
);

alter table public.comments enable row level security;

create policy "public can submit comments"
on public.comments
for insert
to anon
with check (approved = false);

create policy "public can read approved comments"
on public.comments
for select
to anon
using (approved = true);

grant insert, select on table public.comments to anon;
grant usage, select on sequence public.comments_id_seq to anon;
~~~

بهذه السياسات يستطيع الزائر إضافة تعليق جديد بحالة غير معتمدة، وقراءة التعليقات المعتمدة فقط. لا توجد سياسة عامة للتعديل أو الحذف.

## 2. نسخ القيمتين العامتين فقط

من إعدادات مشروع Supabase انسخ:

- **Project URL**
- **anon public key** أو **publishable key** المتاح للاستخدام في المتصفح

لا تنسخ مفتاح service_role.

## 3. وضع القيم في الموقع

افتح الملف:

data/comments-config.js

ثم استبدل النصين الفارغين فقط:

~~~js
export const COMMENTS_CONFIG = Object.freeze({
  supabaseUrl: "ضع Project URL هنا",
  supabaseAnonKey: "ضع المفتاح العام هنا"
});
~~~

ارفع الملف إلى مستودع GitHub وانتظر اكتمال نشر Pages. لا تحتاج إلى تغيير HTML أو CSS أو أي ملف آخر.

## 4. تجربة الإرسال

1. افتح الصفحة الرئيسية المنشورة.
2. أرسل تعليقًا تجريبيًا.
3. يجب أن تظهر رسالة: «شكرًا لك. تم استلام تعليقك للمراجعة.»
4. لن يظهر التعليق للعامة فورًا، لأن القيمة الافتراضية هي approved = false.

## 5. اعتماد تعليق أو حذفه

1. افتح Supabase ثم **Table Editor → comments**.
2. اقرأ الاسم والتعليق.
3. غيّر approved إلى true للتعليق المناسب.
4. احذف التعليق المزعج من لوحة الإدارة عند الحاجة.

بعد اعتماد التعليق سيظهر في الصفحة الرئيسية عند تحديثها. تعرض الصفحة أحدث 5 تعليقات أولًا، ويمكن للزائر الضغط على «عرض المزيد».

## ملاحظات أمان وخصوصية

- النموذج يجمع الاسم والتعليق فقط.
- النصوص المعروضة تعامل كنص عادي، ولا يُسمح بتشغيل HTML أو JavaScript منها.
- مفتاح anon أو المفتاح القابل للنشر ليس مفتاح إدارة، لكن الحماية تعتمد أيضًا على إبقاء RLS مفعّلًا والسياسات السابقة صحيحة.
- لا تضع مفاتيح الإدارة أو كلمات المرور أو رموز GitHub في ملفات الموقع.
- قد تسجّل خدمة الاستضافة معلومات تشغيلية معتادة مثل عنوان IP في سجلات البنية التحتية؛ الموقع نفسه لا يضيف تحليلات أو تتبعًا.
- إذا حذفت القيمتين من ملف الإعداد، يعود الموقع تلقائيًا إلى الحفظ المحلي على جهاز كل زائر.
