# حل المشكلات

ابدأ بتحديد المكان: رسالة عربية داخل تصميم الموقع تعني أن الصفحة وصلت وتحتاج فحص البيانات. صفحة GitHub 404 تعني مشكلة عنوان أو نشر أو مسار ملف. «الكتاب غير موجود» ليست 404 من الاستضافة؛ تعني معرّفًا مفقودًا أو غير صالح أو مخفيًا.

| العَرَض | السبب المحتمل | الإصلاح المباشر |
| --- | --- | --- |
| «تعذر تحميل الكتب» | `books.json` مفقود أو ليس JSON صالحًا | المستودع → `data → books.json → Edit`. افحص وجود المصفوفة `[ ... ]`، ثم صحّح أو استرجع نسخة صحيحة. اضغط «إعادة المحاولة» بعد النشر. |
| خطأ JSON عند الاستيراد | اقتباس مفرد، تعليق، مفتاح مكرر، أو فاصلة خاطئة | افتح الملف كنص. استعمل `"` للنصوص؛ احذف التعليقات وتكرار المفتاح، واجعل الفصل بين كائنين `}, {`، ولا تضع فاصلة بعد الأخير. الأداة تعرض الملف والحقل أو وصف خطأ الصيغة. |
| اختفاء كتاب ورسالة معرّف مكرر | سجلان لهما نفس `id` | راجع `books.json` وابحث عن القيمة. اختر معرّفًا مختلفًا للسجل الجديد فقط، ثم راجع `book_id` في أحداثه. لا يُختار أحد الكتابين المكررين عشوائيًا. |
| خطأ معرّف حدث مكرر | تكرار `id` في `updates.json` | احتفظ بالأحداث الحقيقية، وغيّر معرّف الحدث الجديد إلى لاحقة فريدة مثل `-02`. |
| حدث مرتبط بكتاب غير موجود | خطأ `book_id` أو كتاب غير صالح | انسخ `id` الصحيح حرفيًا من `books.json`. أصلح الكتاب إن كان غير صالح؛ لا تحذف الحدث كحل افتراضي. |
| خطأ `status` أو `type` | استخدام الترجمة العربية أو خطأ إملائي كمفتاح | اختر من القوائم في الأداة. مثال حالة: `final_review`، ومثال حدث: `release`. راجع جدولي الأدلة. |
| خطأ تاريخ | تنسيق غير `YYYY-MM-DD` أو يوم مستحيل | استخدم مثلًا `2026-09-11`؛ `2026-02-30` غير صالح. لا تضع وقتًا أو شرطات مائلة. |
| خطأ نسبة أو عدم ظهورها | نص مثل `"65%"` أو قيمة خارج المجال أو `null` | أدخل `65` كرقم. `null` يعني غير معروفة ويخفي النسبة؛ `0` يظهر 0%. الحد الأعلى 100. |
| كتاب لا يظهر ولا توجد أخطاء | `enabled: false` أو فلاتر نشطة | أعد ضبط البحث. راجع `enabled` في السجل واجعله `true` إذا أردت عرضه. إذا أخفيته تبقى بيانات JSON عامة. |
| «الكتاب غير موجود» | رابط دون `id`، أو معرّف مجهول/معطّل | افتح الكتاب من الفهرس وانسخ رابط «تفاصيل الكتاب». مثال الشكل: `book.html?id=serway-physics-9`. لا تغيّر المعرّف بسبب تغيير العنوان. |
| السجل غير متاح والفهرس يعمل | فشل تحميل `updates.json` أو صيغة المصفوفة | أصلح الملف في `data` ثم أعد تحميل الصفحة. لا تفترض أن عدد الأحداث صفر؛ التاريخ المعروض مؤقتًا من سجل الكتاب وحده. |
| الأحداث متساوية التاريخ تظهر بترتيب غير متوقع | ترتيب المصفوفة | ضع الحدث الذي أدخلته أحدث في بداية `updates.json`. اللواحق `-01` و`-02` لا تستبدل ترتيب المصفوفة. |
| إصدار الكتاب لم يتغير بعد إضافة حدث | الحدث لا يغيّر البيانات الحالية | عدّل `version` و`drive_url` والحالة/النسبة المناسبة في `books.json`، ثم `last_updated` لأن السجل نفسه تغيّر. |
| لا يوجد رابط في حدث قديم | `drive_url` فارغ لذلك الحدث | أضف رابط **ملف ذلك الإصدار** إن كان موجودًا. لا تنسخ رابط النسخة الحالية إلى الماضي تلقائيًا. |
| الغلاف البديل يظهر | صورة غير مرفوعة أو اختلاف حالة حرف | المستودع → `assets → covers`؛ قارن الاسم الكامل بالمسار في `cover`. `Book.webp` يختلف عن `book.webp`. ارفع الصورة أولًا ثم صحح المسار. |
| حتى الغلاف البديل لا يظهر | الملف `placeholder.svg` مفقود | أعد رفع `assets/covers/placeholder.svg` من الحزمة. لا تُصلح ذلك بتكرار رابط مكسور. |
| الصور أو JSON تفشل فقط في مشروع فرعي | مسار يبدأ بـ `/` أو مجلدات في موضع خاطئ | احذف التعديلات التي أدخلت `/assets` أو `/data`. استرجع ملفات JS الأصلية التي تحسب قاعدة الموقع من `common.js`. لا تضع عنوان النشر داخل مسارات الأصول. |
| 404 بعد رفع الملفات | مجلد إضافي أو رفع ZIP فقط | في `Code` يجب أن ترى `index.html` مباشرة. أعد رفع محتويات ZIP بعد فكه إلى الجذر؛ المجلد الصحيح يحتوي `assets/` و`data/` و`tools/`. لا تجعلها `book-review/book-review/index.html`. |
| الصفحة المحلية فارغة أو خطأ تحميل | فُتحت عبر `file://` | انشرها على GitHub Pages وافتح الرابط الفعلي. تحميل وحدات JavaScript وJSON يتطلب HTTP(S)؛ النقر المزدوج ليس المعاينة المدعومة. |
| ظهور HTML كنص عند فتح الأداة | فتحها في مستودع GitHub بدل Pages | افتح الموقع المنشور ثم رابط «أداة صيانة البيانات» في التذييل. |
| الأداة لا تبدأ أو تحاول استيراد ملف واحد | أحد الملفين مفقود | اختر `books.json` و`updates.json` في خانتيهما، ثم «استيراد الملفين». فشل التحميل لا يُنشئ جلسة فارغة. |
| الأداة تمنع التصدير بعد الكتابة | النموذج لم يُحفظ في الذاكرة | اضغط «حفظ الكتاب في الذاكرة» أو «إضافة التحديث إلى الذاكرة»، ثم تحقق ونزّل من جديد. |
| الأداة تمنع تغيير رابط القراءة | الرابط القديم غير محفوظ في إصدار تاريخي | أضف حدث إصدار للملف القديم بتاريخ نسخته ورابطه الحقيقيين، أو أصلح حدثه السابق يدويًا ثم أعد الاستيراد. حافظ على التعليقات الأصلية. |
| النسخ التلقائي لا يعمل | قيود الحافظة في المتصفح أو HTTP | استخدم النص المحدد واضغط `Ctrl+C`، أو نزّل الملفين. لا تحتاج منح أي صلاحية لحساب خارجي. |
| رفع البيانات أنشأ أسماء مختلفة | المتصفح نزّل `books (1).json` | أعد تسمية الملفين إلى `books.json` و`updates.json` قبل رفعهما داخل `data`. |
| Drive يطلب وصولًا أو لا يظهر زر تعليق | الرابط خطأ أو الدور Viewer أو سياسة حساب | الملف → `Share` → راجع `General access` و`Commenter` إذا سمح الحساب. اختبر من حساب قارئ مستقل، لا حساب المالك. الموقع لا يضمن التعليق المجهول. [دليل Drive](GOOGLE_DRIVE_SETUP.md). |
| PDF جديد فقد التعليقات المتوقعة | رفع ملف جديد لا ينقل نقاش الملف القديم | افتح رابط الأرشيف الأصلي؛ لا تحذفه. انقل الملاحظات المفتوحة تنظيميًا مع النسخة/الصفحة. لا تفترض أن تنزيل PDF يضم النقاش. |
| الموقع لم يُنشر | مصدر Pages غير مضبوط | `Settings → Pages → Source: Deploy from a branch → main → /(root) → Save`. افتح الرابط الذي تعرضه GitHub، لا رابطًا تخمينيًا. |
| فشل نشر ظاهر في Actions | ملف دخول مفقود أو إعداد نشر/فرع غير صحيح | `Actions` → آخر تشغيل Pages → افتح المرحلة الفاشلة واقرأ الرسالة. تحقق من `index.html` في الجذر و`.nojekyll` و`Settings → Pages`. أصلح السبب ثم احفظ التغيير. يمكنك إعادة تشغيل العملية الفاشلة بعد الإصلاح إذا أتاحت GitHub ذلك. [إعداد النشر وتشخيصه](https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site). |
| التحديث قديم بعد Commit | النشر لم يكتمل أو ذاكرة المتصفح | تحقق من نجاح آخر نشر أولًا، ثم أعد تحميل الصفحة بـ `Ctrl+Shift+R`، أو جرّب نافذة جديدة. الأداة تعيد التحقق من JSON دون عشوائية في الروابط؛ لا تستبدل البيانات القديمة قبل التأكد. |
| تمرير أفقي على الهاتف | مسار/نص طويل بعد تعديل CSS أو عناصر بعرض ثابت | استرجع `assets/css/style.css` ثم افحص عرض 320 و375 و768 و1440 والتكبير 200%. تجنب العرض الثابت للمحتوى، وحافظ على `min-inline-size: 0` و`overflow-wrap: anywhere` والتفاف عناصر التحكم. |

## استرجاع ملف واحد دون فقد عمل آخر

1. افتح الملف المطلوب في المستودع واضغط `History`.
2. اختر التزامًا معروفًا أنه صحيح. اعرض نسخة الملف في ذلك الالتزام، واضغط `Raw`، ثم انسخ النص القديم كاملًا.
3. ارجع إلى **الملف الحالي على الفرع `main`**. لا تحرر الصفحة التي تعرض الالتزام القديم.
4. اضغط `Edit` واستبدل محتوى هذا الملف فقط بالنص الذي نسخته. راجع الفرق، ثم `Commit changes` برسالة توضّح الاسترجاع.
5. إذا كان الملف من JSON، تحقّق منه مع الملف الآخر الحالي في الأداة قبل النشر متى أمكن. قد تُنشئ استعادة كتب قديمة أحداثًا يتيمة؛ أصلح العلاقات دون مسح أعمال أخرى.

قبل تغييرات واسعة، المستودع → `Code → Download ZIP` لحفظ نسخة مصدر. لا يحتوي هذا النسخ الاحتياطي على PDFs أو تعليقات Drive. احتفظ منفصلًا بمصدر الكتب القابل للتحرير ونسخ PDF المنشورة. `enabled: false` ليس آلية خصوصية؛ أي بيانات تُرفع إلى المستودع العام يمكن الوصول إليها.

## الفحص بعد إصلاح المشكلة

افتح الرئيسية، جرّب البحث وإعادة الضبط، ثم صفحة الكتاب وسجلّه. تحقق من النسبة والتاريخ والنسخة والرابط الصحيح. اختبر Drive بحساب القارئ. راجع [تقرير الاختبار](TEST_REPORT.md) لمعرفة الفحوص التي نفذت محليًا وما بقي بحاجة إلى متصفحك أو حسابك.
