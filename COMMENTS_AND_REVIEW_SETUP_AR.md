# تفعيل التعليقات العامة ومراجعات الكتب المشتركة

هذا الإعداد يُنفَّذ مرة واحدة فقط. بعده:

- تُحفظ التعليقات العامة في مكان مشترك وتظهر لكل الزوار بعد اعتمادها.
- تصل ملاحظات الكتب التفصيلية إلى جدول خاص بالإدارة.
- يبقى بريد المراجع خاصًا ولا يظهر في الصفحة.
- يمكن إرسال إشعار إلى بريد مدير المشروع عند وصول مشاركة جديدة.

## لماذا يلزم هذا الإعداد؟

GitHub Pages يعرض ملفات ثابتة ولا يملك قاعدة بيانات تحفظ ما يكتبه الزوار. أُعد الموقع للعمل مع Supabase لهذا الغرض. إذا بقيت بيانات Supabase فارغة، يستخدم الموقع حفظًا محليًا احتياطيًا على جهاز الزائر، وهذا الحفظ لا يظهر للآخرين ولا يصل إلى فريق المشروع.

## الجزء الأول: إنشاء قاعدة البيانات

1. أنشئ مشروعًا في Supabase.
2. افتح **SQL Editor** ثم اختر **New query**.
3. الصق النص التالي كاملًا، ثم اختر **Run**:

~~~sql
create table if not exists public.general_comments (
  id bigint generated by default as identity primary key,
  name text not null check (char_length(btrim(name)) between 2 and 60),
  comment text not null check (char_length(btrim(comment)) between 3 and 1000),
  created_at timestamptz not null default now(),
  approved boolean not null default false
);

create table if not exists public.book_feedback (
  id bigint generated by default as identity primary key,
  book_id text not null check (char_length(btrim(book_id)) between 1 and 160),
  book_title text not null check (char_length(btrim(book_title)) between 1 and 500),
  reviewer_name text not null check (char_length(btrim(reviewer_name)) between 2 and 60),
  reviewer_email text check (
    reviewer_email is null
    or (
      char_length(btrim(reviewer_email)) between 3 and 254
      and btrim(reviewer_email) ~* '^[^[:space:]@]+@[^[:space:]@]+\.[^[:space:]@]+$'
    )
  ),
  page_number text not null check (char_length(btrim(page_number)) between 1 and 20),
  issue_type text not null check (issue_type in (
    'خطأ في الترجمة',
    'مصطلح علمي',
    'خطأ علمي',
    'معادلة أو رمز',
    'شكل أو صورة',
    'تعليق على الشكل',
    'خطأ إملائي',
    'خطأ نحوي أو لغوي',
    'تنسيق الصفحة',
    'نص غير واضح',
    'جزء مفقود',
    'مرجع أو اقتباس',
    'اقتراح لتحسين الأسلوب',
    'ملاحظة عامة',
    'أخرى'
  )),
  comment text not null check (char_length(btrim(comment)) between 5 and 3000),
  suggested_correction text check (
    suggested_correction is null
    or char_length(suggested_correction) <= 3000
  ),
  wants_contact boolean not null default false,
  created_at timestamptz not null default now(),
  approved boolean not null default false,
  status text not null default 'new' check (
    status in ('new', 'reviewing', 'accepted', 'applied', 'rejected')
  ),
  constraint contact_requires_email check (
    not wants_contact or reviewer_email is not null
  )
);

create index if not exists general_comments_public_order_idx
  on public.general_comments (approved, created_at desc);

create index if not exists book_feedback_work_queue_idx
  on public.book_feedback (status, created_at desc);

alter table public.general_comments enable row level security;
alter table public.book_feedback enable row level security;

revoke all on table public.general_comments from anon, authenticated;
revoke all on table public.book_feedback from anon, authenticated;

grant select (id, name, comment, created_at, approved)
  on table public.general_comments to anon, authenticated;
grant insert (name, comment)
  on table public.general_comments to anon, authenticated;

grant insert (
  book_id,
  book_title,
  reviewer_name,
  reviewer_email,
  page_number,
  issue_type,
  comment,
  suggested_correction,
  wants_contact
) on table public.book_feedback to anon, authenticated;

revoke all on sequence public.general_comments_id_seq from anon, authenticated;
revoke all on sequence public.book_feedback_id_seq from anon, authenticated;
grant usage on sequence public.general_comments_id_seq to anon, authenticated;
grant usage on sequence public.book_feedback_id_seq to anon, authenticated;

drop policy if exists public_read_approved_general_comments
  on public.general_comments;
create policy public_read_approved_general_comments
  on public.general_comments
  for select
  to anon, authenticated
  using (approved = true);

drop policy if exists public_submit_general_comment
  on public.general_comments;
create policy public_submit_general_comment
  on public.general_comments
  for insert
  to anon, authenticated
  with check (approved = false);

drop policy if exists public_submit_book_feedback
  on public.book_feedback;
create policy public_submit_book_feedback
  on public.book_feedback
  for insert
  to anon, authenticated
  with check (approved = false and status = 'new');
~~~

النص نفسه محفوظ أيضًا في:

    supabase/migrations/20260914000000_participation_tables.sql

الصلاحيات السابقة مقصودة:

- يستطيع الزائر إضافة تعليق عام، لكنه لا يستطيع اعتماده بنفسه.
- يستطيع الزائر قراءة التعليقات العامة التي اعتمدتها الإدارة فقط.
- يستطيع الزائر إرسال ملاحظة كتاب، لكنه لا يستطيع قراءة ملاحظات الكتب أو رسائل البريد.
- لا يستطيع الزائر تعديل السجلات أو حذفها.

## الجزء الثاني: ربط الموقع

1. في لوحة Supabase افتح إعدادات المشروع ثم قسم API.
2. انسخ **Project URL**.
3. انسخ **Publishable key**، أو مفتاح **anon public** في المشروعات القديمة.
4. افتح الملف:

    data/comments-config.js

5. ضع القيمتين العامتين فقط:

~~~js
export const COMMENTS_CONFIG = Object.freeze({
  supabaseUrl: "https://PROJECT_REF.supabase.co",
  supabaseAnonKey: "ضع_المفتاح_العام_هنا"
});
~~~

المفتاح العام مسموح في واجهة الموقع عندما تكون سياسات RLS مفعلة كما في SQL أعلاه. **لا تضع مطلقًا** مفتاح service_role أو Secret key أو كلمة مرور قاعدة البيانات داخل ملفات الموقع.

## الجزء الثالث: تجربة التعليقات العامة

1. ارفع الملفات إلى GitHub Pages.
2. افتح الصفحة الرئيسية وأرسل تعليقًا تجريبيًا.
3. يجب أن تظهر رسالة: «تم استلام تعليقك للمراجعة».
4. افتح جدول `general_comments` في Supabase؛ ستجد `approved = false`.
5. اقرأ التعليق. إذا كان مناسبًا، غيّر `approved` إلى `true`.
6. حدّث الصفحة الرئيسية. سيظهر التعليق لكل الزوار.

## الجزء الرابع: تجربة ملاحظة كتاب

1. افتح أي صفحة كتاب.
2. اختر **أرسل ملاحظة على هذا الكتاب**.
3. اكتب رقم الصفحة والنوع والشرح ثم أرسل.
4. افتح جدول `book_feedback` في Supabase.
5. استخدم `status` لتنظيم العمل:

| القيمة | معناها |
|---|---|
| `new` | جديدة |
| `reviewing` | قيد الفحص |
| `accepted` | مقبولة |
| `applied` | طُبّق التصحيح |
| `rejected` | لم تُقبل |

الحقل `approved` في ملاحظات الكتب للاستخدام الإداري فقط؛ الملاحظات التفصيلية لا تُعرض للعامة في الموقع. لا تنسخ `reviewer_email` إلى أي صفحة عامة.

## الجزء الخامس: إشعارات البريد الاختيارية

التعليقات والملاحظات المشتركة تعمل من دون إشعار بريدي. لتلقي رسالة عند كل مشاركة جديدة، أُرفقت دالة Supabase Edge Function جاهزة في:

    supabase/functions/notify-submission/index.ts

تحتاج إلى حساب Resend ونطاق بريد موثّق لديه.

### 1. أسرار الدالة

أضف القيم التالية من صفحة **Edge Function Secrets** في Supabase:

| الاسم | القيمة |
|---|---|
| `RESEND_API_KEY` | مفتاح Resend السري |
| `ADMIN_EMAIL` | البريد الذي يستقبل الإشعارات |
| `FROM_EMAIL` | مرسل من النطاق الموثّق، مثل `Reviews <reviews@example.com>` |
| `WEBHOOK_SECRET` | نص عشوائي طويل تنشئه أنت |

لا تضع أيًا من هذه القيم في `data/comments-config.js`، ولا ترفع ملف `.env` إلى GitHub.

### 2. نشر الدالة

من مجلد المشروع، وبعد تثبيت Supabase CLI وربطه بمشروعك:

~~~bash
supabase functions deploy notify-submission --no-verify-jwt
~~~

التحقق من JWT معطل لهذه الدالة لأن الطلب يأتي من Database Webhook، لكن الدالة ترفض أي طلب لا يحمل `WEBHOOK_SECRET` الصحيح.

### 3. إنشاء خطافي قاعدة البيانات

في Supabase افتح **Database Webhooks** وأنشئ خطافين:

1. خطاف لحدث `INSERT` على `public.general_comments`.
2. خطاف لحدث `INSERT` على `public.book_feedback`.

استخدم العنوان نفسه لكليهما:

    https://PROJECT_REF.supabase.co/functions/v1/notify-submission

وأضف ترويسة HTTP:

    x-webhook-secret: نفس قيمة WEBHOOK_SECRET

اختبر بتعليق عام ثم بملاحظة كتاب. تصل الرسالة إلى `ADMIN_EMAIL`. الإشعار لا يعتمد التعليق ولا ينشره؛ يبقى الاعتماد يدويًا.

## الخصوصية والحماية

- يجمع نموذج التعليق العام الاسم والتعليق فقط.
- بريد المراجع اختياري، ويصبح مطلوبًا فقط إذا طلب التواصل.
- الواجهة لا تعرض البريد ولا الملاحظات التفصيلية.
- تُعرض نصوص الزوار باستخدام `textContent` ولا تُنفذ كـ HTML.
- يوجد تأخير قصير بين الإرسالات في المتصفح، بينما تمنع المراجعة المسبقة ظهور الإساءة مباشرة.
- قد تحتفظ Supabase وResend بسجلات تشغيل وببيانات تقنية لازمة لتقديم الخدمة وفق سياساتهما؛ الموقع لا يضيف تحليلات أو تتبعًا مخصصًا.

## إذا لم يعمل الاتصال

- تأكد أن الرابط يبدأ بـ `https://` وأن المفتاح هو Publishable/anon العام.
- تأكد أنك نفذت SQL كاملًا.
- افتح أدوات المطور وابحث عن خطأ `401` أو `403`؛ غالبًا سببه مفتاح غير صحيح أو سياسة ناقصة.
- إذا ظهرت رسالة الحفظ على الجهاز، فهذا يعني أن الموقع انتقل إلى الوضع الاحتياطي ولم تصل المشاركة إلى Supabase.

## مراجع رسمية

- [Supabase: Row Level Security](https://supabase.com/docs/guides/database/postgres/row-level-security)
- [Supabase: Database Webhooks](https://supabase.com/docs/guides/database/webhooks)
- [Supabase: Edge Function Secrets](https://supabase.com/docs/guides/functions/secrets)
- [Resend: إرسال البريد من Supabase Edge Functions](https://resend.com/docs/send-with-supabase-edge-functions)
