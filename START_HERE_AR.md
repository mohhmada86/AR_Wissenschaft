# ابدأ من هنا — إضافة الكتب الحقيقية

هذه النسخة نظيفة: لا توجد كتب أو تحديثات تجريبية في الموقع.

## عند إضافة كل كتاب

1. جهّز **PDF الحالي** وارفعه إلى Google Drive، ثم اضبط المشاركة بحيث يستطيع القارئ فتحه، واختبر الرابط.
2. جهّز **صورة الغلاف** باسم إنجليزي بسيط مثل `serway-physics-9.webp` وارفعها إلى `assets/covers/`.
3. افتح أداة الصيانة من الموقع المنشور: `tools/update-helper.html`.
4. اضغط **تحميل أحدث البيانات المنشورة**.
5. اختر **إضافة أو تعديل كتاب → كتاب جديد**.
6. املأ بيانات الكتاب الحقيقية. أهم الحقول:
   - `id`: معرّف ثابت وفريد مثل `serway-physics-9`؛ لا تغيّره لاحقًا.
   - `title_ar`: العنوان العربي.
   - `title_en`: العنوان الأصلي/الإنجليزي إن وجد.
   - `author`: المؤلف.
   - `category`: التخصص؛ مثل فيزياء، رياضيات، لغات، إلكترونيات.
   - `cover`: مثل `assets/covers/serway-physics-9.webp`.
   - `status`: حالة العمل الحالية.
   - `version`: إصدار مشروعك مثل `v1.0`.
   - `progress`: نسبة المراجعة من 0 إلى 100، أو `null` إذا لم تحددها.
   - `last_updated`: التاريخ بصيغة `YYYY-MM-DD`.
   - `description`: وصف قصير واضح.
   - `drive_url`: رابط Google Drive للـ PDF الحالي.
   - `enabled`: اجعله `true`.
   - `demo`: اجعله **false دائمًا للكتب الحقيقية**.
7. اضغط **حفظ الكتاب في الذاكرة**.
8. إذا كان هذا أول PDF فعلي للكتاب، أضف تحديثًا من نوع `release` بنفس `book_id` و`version` ورابط الـ PDF.
9. اضغط **التحقق وتجهيز JSON** ثم نزّل `books.json` و`updates.json`.
10. ارفع الملفين معًا إلى مجلد `data/` في GitHub، ثم `Commit changes`.
11. انتظر GitHub Pages قليلًا، وافتح الموقع وتأكد من: الغلاف، العنوان، حالة الكتاب، نسبة المراجعة، رابط القراءة، وصفحة سجل التحديثات.

## حالات الكتب المتاحة

- `review` = قيد المراجعة
- `translation` = قيد الترجمة
- `correction` = قيد التصحيح
- `final_review` = مراجعة نهائية
- `complete` = مكتمل
- `paused` = متوقف مؤقتًا
- `coming_soon` = قريبًا

## قاعدة مهمة للإصدارات

عندما تنشر PDF جديدًا لكتاب موجود، **لا تستبدل تاريخ النسخة القديمة فقط**. احتفظ برابط النسخة القديمة داخل حدثها في `updates.json`، ثم ضع رابط النسخة الجديدة في سجل الكتاب وأضف حدث `release` جديدًا. هكذا يبقى تاريخ كل إصدار محفوظًا.

## ما الذي تغيّر في هذه النسخة؟

- `data/books.json` أصبح `[]`.
- `data/updates.json` أصبح `[]`.
- أزيلت الكتب والأحداث التجريبية من البيانات الحية.
- بقي `assets/covers/placeholder.svg` كغلاف احتياطي تقني فقط، وليس كتابًا تجريبيًا.
