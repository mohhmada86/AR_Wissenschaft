# تحديث كتاب موجود

## المسار اليومي الموصى به

1. حمّل أحدث `books.json` و`updates.json` في [الأداة](tools/update-helper.html). لا تبدأ من ملفات قديمة. قبل تغيير كبير نزّل نسخة مصدر من المستودع → `Code → Download ZIP`.
2. إذا أضفت PDF جديدًا، احتفظ بالملف السابق وتعليقاته، وارفع الجديد كملف مستقل واختبر مشاركته. تأكد أن رابط القديم محفوظ في حدث إصداره. [الخطوات الكاملة في Drive](GOOGLE_DRIVE_SETUP.md).
3. «إضافة أو تعديل كتاب» → اختر الكتاب → حدّث الحقول المطلوبة و`last_updated` عند تغيير السجل → «حفظ الكتاب في الذاكرة».
4. «إضافة تحديث» → اختر الكتاب نفسه → أدخل الحدث → «إضافة التحديث إلى الذاكرة». للأحداث المرتبطة بنسخة جديدة، اختر `release` وضع رقم النسخة ورابط ملفها الجديد. لا يُغيّر هذا الحدث بيانات الكتاب تلقائيًا.
5. «التحقق وتجهيز JSON» → راجع ثم نزّل **الملفين**. ارفع أي غلاف جديد أولًا. GitHub → المستودع → `data → Add file → Upload files` → ارفع الملفين كاملين واستبدل النسختين الحاليتين → راجع الفرق → `Commit changes`.
6. انتظر النشر ثم تحقق من البطاقة وصفحة التفاصيل وروابط التاريخ. ملفات التنزيل وحدها لا تغيّر الموقع.

المعرّفات القديمة مقفلة داخل الأداة. لا توجد وظيفة لحذف كتاب أو سجل قديم؛ استخدم `enabled: false` للإخفاء. إذا أردت إصلاح حدث قديم ناقص الرابط، حرّر ذلك الحدث نفسه في `updates.json` ثم حمّل أحدث الملفين؛ أو أضف حدث إصدار أرشيفي بالتاريخ والرابط الحقيقيين. عند محاولة تغيير رابط حالي غير مؤرشف، توقف الأداة الحفظ وتشرح المطلوب.

## التعديل اليدوي: أين أضغط؟

المستودع → `data → books.json` → أيقونة القلم `Edit` → ابحث عن `id` الدائم → عدّل الحقل في **هذا الكائن فقط** → `Commit changes`.

| التغيير | الحقل والقيمة |
| --- | --- |
| رابط القراءة | `drive_url`: رابط ملف PDF الحقيقي كما نسخته من Drive |
| الإصدار | `version`: مثل `v1.1` |
| الحالة | `status`: مثل `correction` أو `final_review` أو `complete` |
| نسبة المراجعة | `progress`: مثل `85` دون `%`، أو `null` |
| الوصف | `description`: النص الجديد بين تنصيص مزدوج |
| الغلاف | `cover`: مثل `assets/covers/language-learning-notes.webp` بعد رفع الملف |
| تاريخ تغيير السجل | `last_updated`: تاريخ اليوم الذي أجريت فيه التغيير فعليًا |

تغيير الحالة يدوي، وليس عدًّا لتعليقات Drive. رقم الإصدار يخص مراجعة المشروع: `v1.1`/`v1.2` لتصحيحات متدرجة، و`v2.0` عند إعادة عمل كبيرة. لا تُلزم كل جولة مراجعة بقفزة رئيسية.

## مثال قبل/بعد كامل

المثال يشرح ترابط السجلات؛ الروابط فارغة عمدًا ولا توجد ملفات حقيقية مرتبطة. عند تطبيق **نشر PDF حقيقي** ضع الرابط القديم الفعلي في حدثه القديم، والرابط الجديد الفعلي في الكتاب والحدث الجديد. لا تستبدل ملفًا متعدد الكتب بمثال الكتاب الواحد.

قبل — محتوى `books.json` إذا كان المشروع يحتوي هذا الكتاب وحده:

```json
[
  {
    "id": "language-learning-notes",
    "title_ar": "ملاحظات لتعلم اللغات",
    "title_en": "Language Learning Notes",
    "author": "اسم المؤلف",
    "category": "لغات",
    "cover": "assets/covers/placeholder.svg",
    "status": "review",
    "version": "v1.0",
    "progress": 20,
    "last_updated": "2026-09-01",
    "description": "النسخة الأولى جاهزة للمراجعة.",
    "drive_url": "",
    "enabled": true,
    "demo": false
  }
]
```

قبل — محتوى `updates.json`:

```json
[
  {
    "id": "language-learning-notes-2026-09-01-01",
    "book_id": "language-learning-notes",
    "date": "2026-09-01",
    "version": "v1.0",
    "type": "release",
    "title": "نشر النسخة الأولى",
    "description": "بدء المراجعة الجماعية للنسخة الأولى.",
    "drive_url": ""
  }
]
```

بعد — السجل الحالي في `books.json`:

```json
[
  {
    "id": "language-learning-notes",
    "title_ar": "ملاحظات لتعلم اللغات",
    "title_en": "Language Learning Notes",
    "author": "اسم المؤلف",
    "category": "لغات",
    "cover": "assets/covers/placeholder.svg",
    "status": "final_review",
    "version": "v1.1",
    "progress": 85,
    "last_updated": "2026-09-11",
    "description": "نُقّحت المصطلحات والأمثلة، وتنتظر النسخة مراجعة نهائية.",
    "drive_url": "",
    "enabled": true,
    "demo": false
  }
]
```

بعد — `updates.json` مع الحدث الجديد **في البداية** والحدث القديم محفوظًا:

```json
[
  {
    "id": "language-learning-notes-2026-09-11-01",
    "book_id": "language-learning-notes",
    "date": "2026-09-11",
    "version": "v1.1",
    "type": "release",
    "title": "نشر النسخة المنقحة",
    "description": "تصحيح المصطلحات والأمثلة وبدء المراجعة النهائية.",
    "drive_url": ""
  },
  {
    "id": "language-learning-notes-2026-09-01-01",
    "book_id": "language-learning-notes",
    "date": "2026-09-01",
    "version": "v1.0",
    "type": "release",
    "title": "نشر النسخة الأولى",
    "description": "بدء المراجعة الجماعية للنسخة الأولى.",
    "drive_url": ""
  }
]
```

إذا احتوى مشروعك كتبًا وأحداثًا أخرى، احتفظ بها. في تحديثين في اليوم نفسه استخدم معرّفين مختلفين مثل `language-learning-notes-2026-09-11-02` و`language-learning-notes-2026-09-11-01`، وضع الأحدث إدخالًا في البداية؛ لا يفسر الموقع اللواحق كتوقيت.

## حقول الحدث

| الحقل | القاعدة |
| --- | --- |
| `id` | معرّف دائم فريد بنفس قاعدة معرّف الكتاب، مع لاحقة تميز الأحداث |
| `book_id` | معرّف كتاب موجود وصالح بالضبط |
| `date` | تاريخ فعلي صالح بصيغة `YYYY-MM-DD` |
| `version` | `vMAJOR.MINOR` أو `""` لحدث لا يذكر إصدارًا |
| `type` | مفتاح من الجدول التالي |
| `title`, `description` | نصان غير فارغين؛ لا تضع HTML لتنسيقهما |
| `drive_url` | رابط الملف الخاص بهذا الحدث أو `""`؛ يجوز غياب الحقل، ولا يُملأ من الرابط الحالي |

| النوع | الاستخدام |
| --- | --- |
| `release` | نشر إصدار |
| `correction` | تسجيل تصحيح |
| `progress` | إعلان تقدم |
| `status` | توثيق انتقال حالة |
| `note` | ملاحظة عامة |

أضف الحدث من المستودع → `data → updates.json → Edit` → بعد `[` مباشرة. ضع فاصلة بينه وبين الحدث الذي يليه، وليس بعد آخر حدث. احتفظ بكل الروابط والأحداث الأقدم.

## متى أعدّل التاريخ؟

- عدّلت بيانات الكتاب مباشرة، حتى الوصف أو الغلاف فقط: عدّل `last_updated` إلى تاريخ التغيير الحقيقي. لا يلزم رفع النسخة لتصحيح بطاقة الموقع.
- أضفت حدثًا فقط دون تغيير بيانات الكتاب: يكفي `date` في الحدث؛ التاريخ الظاهر يتقدم تلقائيًا إذا كان أحدث. لا تصنع صيانة مزدوجة للتاريخ.
- أحدث تاريخ ظاهر = الأكبر بين `last_updated` وتواريخ أحداث الكتاب الصالحة. إذا أضفت اليوم حدثًا مؤرخًا بأسبوع سابق فلن يصبح الأحدث لمجرد موضعه في المصفوفة.
- `updates.json` سجل تاريخي؛ لا يحدد الحالة الحالية أو النسبة أو الإصدار أو رابط القراءة. قد تكتب حدثًا عن `v1.0` والكتاب حاليًا `v1.2`، ويبقى الحالي `v1.2`.

## PDF جديد أم تصحيح رابط؟

**محتوى PDF جديد:** احتفظ بالملف القديم وتعليقاته، وارفع الجديد كملف جديد. أرشف رابط النسخة القديمة في حدثها، ثم غيّر `drive_url` و`version` في الكتاب وأضف `release` جديدًا برابط الجديد. راجع إعدادات مشاركة كل ملف منفصلًا.

**الرابط مكتوب خطأ أو تغيرت صيغة مشاركة الملف نفسه:** تأكد أنه **نفس الملف ونفس المحتوى والإصدار**. صحّح الرابط الحالي و`last_updated`؛ لا ترفع النسخة لمجرد إصلاح الرابط. صحّح الرابط التاريخي فقط إذا كان هو الآخر خطأ ويُفترض أن يشير إلى هذا الملف نفسه. لا توجّه حدث `v1.0` بصمت إلى PDF مختلف من `v1.1`. يمكن إضافة `note` يشرح تصحيح الرابط. إذا لم يتغير نص الرابط وإنما أصلحت الصلاحيات في Drive، اختبر القراءة والتعليق؛ لا يلزم تعديل JSON إلا لتوثيق حدث مفيد.

الأداة لا تختبر هوية الملف على Drive. قد تطلب تأريخ الرابط السابق حتى عند تغيير صيغة الرابط؛ استخدم دليل الملف ومعرّفه الفعلي لتجنب أرشيف مضلل.

## إنهاء الجولة

صحّح المصدر، واحفظ الملاحظات العالقة مع نسختها وصفحتها، ثم اتبع مسار PDF الجديد أعلاه. ضع `complete` و`progress: 100` فقط إذا كانا يعكسان قرارك الفعلي. تظل الأحداث القديمة متاحة. تعليمات `Resolve` و`Manage versions` والنسخ الاحتياطي في [دليل Drive](GOOGLE_DRIVE_SETUP.md).

## مراجعة الفرق والاسترجاع

عند رفع الملفين معًا، راجع أنه لم تُحذف كتب أو أحداث غير مقصودة. إذا لم تُظهر شاشة الرفع فرقًا مفصلًا، قارنهما بالملفين الحاليين قبل الحفظ، أو ارفع إلى فرع جديد وافحص `Files changed` في Pull request ثم ادمجه. تعديل كل ملف بالقلم يمكن أن ينشئ التزامًا منفصلًا؛ لا تجمع GitHub التعديلين تلقائيًا. [رفع الملفات](https://docs.github.com/en/repositories/working-with-files/managing-files/adding-a-file-to-a-repository).

لاسترجاع ملف واحد: افتحه → `History` → اختر التزامًا صحيحًا → اعرض الملف القديم ثم `Raw` وانسخ محتواه → ارجع إلى الملف الحالي على `main` → `Edit` → استبدل المحتوى → راجع → `Commit changes`. لا تُعد المستودع كله إلى الماضي لإصلاح ملف واحد. إذا احتجت استرجاع الملفين، افحص علاقة `book_id` واحتفظ بتعديلاتك الأخرى، ثم استخدم الأداة للتحقق قبل الرفع. [تفاصيل الحلول](TROUBLESHOOTING.md).
