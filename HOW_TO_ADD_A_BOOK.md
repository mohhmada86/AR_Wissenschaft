# إضافة كتاب جديد

## الطريقة الأسهل: أداة الصيانة

1. إن توفر PDF، اضبط مشاركته واختبر التعليق من حساب قارئ، وفق [دليل Drive](GOOGLE_DRIVE_SETUP.md). ارفع الغلاف أولًا إن وجد.
2. افتح [الأداة](tools/update-helper.html) من الموقع المنشور → «تحميل أحدث البيانات المنشورة» → «إضافة أو تعديل كتاب» → «كتاب جديد». املأ الحقول، واختر معرّفًا لم يُستخدم، وألغِ علامة «بيانات تجريبية» للكتاب الحقيقي، ثم «حفظ الكتاب في الذاكرة».
3. عند أول نشر PDF: «إضافة تحديث» → اختر الكتاب → النوع `release` → أدخل تاريخ النشر ونسخته ورابطه الفعليين → احفظ الحدث في الذاكرة.
4. «التحقق وتجهيز JSON» → نزّل `books.json` و`updates.json` → GitHub → المستودع → `data → Add file → Upload files` → ارفع الملفين معًا، راجع المحتوى والاستبدالات ثم `Commit changes`. لا ترفع نسخة قديمة من أحد الملفين.

تحميل البيانات في الأداة ليس تحديثًا من Drive. احرص أن يكون آخر تعديل GitHub قد نُشر قبل تحميلها. إذا أردت استيرادًا محليًا اختر **الملفين** لا أحدهما فقط.

## رفع الغلاف من GitHub

المستودع → `assets → covers → Add file → Upload files` → اختر الصورة → راجع الاسم → `Commit changes`. [دليل GitHub لرفع الملفات](https://docs.github.com/en/repositories/working-with-files/managing-files/adding-a-file-to-a-repository).

استخدم مثلًا `language-learning-notes.webp`، ثم اكتب `assets/covers/language-learning-notes.webp` في حقل الغلاف. الاسم والمسار حساسان لحالة الحروف. الحجم المقترح 600×900، بصيغة WebP أو JPEG، ويفضل أقل من 150 KB؛ الموقع يحفظ نسبة الأبعاد ولا يقص عنوان الغلاف. يُقبل PNG أو SVG محلي أيضًا. أسماء الملفات والمجلدات في مسار الغلاف تستخدم الحروف الإنجليزية والأرقام و`-` و`_` والنقطة، دون مسافات. المسار لا يبدأ بـ `/` ولا يحتوي `..` أو رابط صورة خارجي. استخدم `""` أو `assets/covers/placeholder.svg` للغلاف البديل.

## الإضافة يدويًا

المستودع → `data → books.json` → أيقونة القلم `Edit this file` أو `Edit` → أضف **كائن الكتاب** داخل المصفوفة الموجودة → عدّل حقوله → راجع الفرق → `Commit changes`. لا تستبدل جميع الكتب بقالب كتاب واحد.

هذا قالب سجل قابل لإعادة الاستخدام. غيّر القيم المثالّية، خصوصًا المعرّف والعنوان والمؤلف والتاريخ. الرابط فارغ عمدًا؛ ألصق رابطك الحقيقي قبل إعلان نشر PDF:

```json
{
  "id": "language-learning-notes",
  "title_ar": "ملاحظات لتعلم اللغات",
  "title_en": "Language Learning Notes",
  "author": "اسم المؤلف",
  "category": "لغات",
  "cover": "assets/covers/placeholder.svg",
  "status": "review",
  "version": "v1.0",
  "progress": 0,
  "last_updated": "2026-09-11",
  "description": "اكتب وصفًا موجزًا للكتاب وما يحتاج إلى مراجعة.",
  "drive_url": "",
  "enabled": true,
  "demo": false
}
```

| الحقل | كيف تملؤه |
| --- | --- |
| `id` | دائم وفريد؛ مثل `serway-physics-9`. حروف صغيرة وأرقام وشرطات مفردة، دون شرطة في البداية أو النهاية. لا تغيّره عند تغيير العنوان. |
| `title_ar` | عنوان العرض، نص غير فارغ. |
| `title_en` | عنوان إنجليزي أو `""`؛ يظهر باتجاه LTR. |
| `author` | اسم المؤلف، نص غير فارغ. |
| `category` | أي تخصص غير فارغ. التخصص الجديد يظهر في الفلتر دون تعديل JavaScript. |
| `cover` | مسار محلي صالح أو نص فارغ للبديل. |
| `status` | مفتاح من جدول الحالات أدناه، وليس ترجمة الحالة العربية. |
| `version` | `v1.0` أو `v1.1`… إصدار مراجعة المشروع، لا طبعة الناشر. |
| `progress` | رقم من 0 إلى 100 أو `null`. الصفر يظهر 0%؛ المجهول وحده يُخفى. تقدير يدوي لصاحب المشروع. |
| `last_updated` | يوم آخر تغيير فعلي في سجل الكتاب بصيغة `YYYY-MM-DD`. تاريخ صحيح مثل `2026-09-11`؛ لا تستخدم تاريخًا وهميًا مثل `2026-02-30`. |
| `description` | وصف نصي غير فارغ؛ لا يُفسّر على أنه HTML. |
| `drive_url` | رابط PDF الفعلي HTTPS من Drive، أو `""` إن لم يتوفر. النص `YOUR_GOOGLE_DRIVE_LINK_HERE` غير قابل للنقر أيضًا. |
| `enabled` | `true` لإظهار الكتاب؛ `false` يخفيه من الفهرس والتفاصيل والفلاتر والعدّادات والتحديثات الحديثة. البيانات العامة لا تصبح خاصة. |
| `demo` | `false` لكتاب حقيقي؛ `true` يضع شارة «بيانات تجريبية». |

| `status` | النص الظاهر |
| --- | --- |
| `review` | قيد المراجعة |
| `translation` | قيد الترجمة |
| `correction` | قيد التصحيح |
| `final_review` | مراجعة نهائية |
| `complete` | مكتمل |
| `paused` | متوقف مؤقتًا |
| `coming_soon` | قريبًا |

## مثال كامل صحيح لمصفوفة من كتابين

مثال بنية فقط؛ لا تنشر أسماء الكتب المثالّية وكأنها كتبك الفعلية. `demo: false` هنا يوضح إعداد كتاب حقيقي، وليس إقرارًا بوجود هذين الكتابين. لا توجد روابط أو ملفات حقيقية في المثال:

```json
[
  {
    "id": "language-learning-notes",
    "title_ar": "ملاحظات لتعلم اللغات",
    "title_en": "Language Learning Notes",
    "author": "اسم المؤلف",
    "category": "لغات",
    "cover": "assets/covers/placeholder.svg",
    "status": "review",
    "version": "v1.0",
    "progress": 0,
    "last_updated": "2026-09-11",
    "description": "اكتب وصفًا موجزًا للكتاب وما يحتاج إلى مراجعة.",
    "drive_url": "",
    "enabled": true,
    "demo": false
  },
  {
    "id": "history-reading-notes",
    "title_ar": "ملاحظات قراءة في التاريخ",
    "title_en": "",
    "author": "اسم المؤلف",
    "category": "تاريخ",
    "cover": "assets/covers/placeholder.svg",
    "status": "coming_soon",
    "version": "v1.0",
    "progress": null,
    "last_updated": "2026-09-11",
    "description": "مثال هيكلي لكتاب في تخصص آخر لم تتوفر نسخته بعد.",
    "drive_url": "",
    "enabled": true,
    "demo": false
  }
]
```

## درس الفواصل في دقيقة

- الملف يبدأ بـ `[` وينتهي بـ `]`، حتى عندما يحتوي كتابًا واحدًا. المصفوفة الفارغة `[]` صحيحة عند قصد البدء دون كتب، وليست حلًا لفشل التحميل.
- كل كتاب بين `{` و`}`. بين كتابين: `} {` خطأ؛ `}, {` صحيح.
- لا تضع فاصلة بعد آخر حقل في الكائن أو آخر كتاب في المصفوفة.
- أسماء الحقول والنصوص داخل علامات تنصيص مزدوجة `"`، وليس `'`. إذا احتوى النص اقتباسًا مزدوجًا اهربه بـ `\"`؛ واكتب السطر الجديد داخل النص بـ `\n`.
- `"progress": 65` صحيح؛ `"progress": "65%"` خطأ. `null`, `true`, `false` دون تنصيص. `"null"` نص وليس قيمة مجهولة.
- لا تضف تعليقات إلى JSON ولا تكرر المفتاح نفسه داخل كائن. احفظ UTF-8 ومسافتين للمسافة البادئة؛ الأداة تتولى ذلك باستخدام `JSON.stringify`.

## أول PDF يحتاج حدث إصدار

افتح `data → updates.json → Edit`. أضف هذا الحدث **أول المصفوفة** مع الاحتفاظ بالأحداث القديمة، بعد وضع نفس رابط PDF الموجود في سجل الكتاب. الرابط في المثال فارغ كي لا نخترع رابطًا:

```json
{
  "id": "language-learning-notes-2026-09-11-01",
  "book_id": "language-learning-notes",
  "date": "2026-09-11",
  "version": "v1.0",
  "type": "release",
  "title": "نشر النسخة الأولى",
  "description": "بدء المراجعة الجماعية للنسخة الأولى.",
  "drive_url": ""
}
```

`book_id` يطابق معرّف الكتاب حرفيًا. `id` فريد للحدث؛ استخدم `-01` ثم `-02` إذا احتجت عدة أحداث في اليوم نفسه. إدخال الحدث الجديد أولًا يحفظ أسبقيته عند تساوي التاريخ. عند غياب PDF لا تعلن إصدارًا فعليًا؛ اترك الكتاب بحالته المناسبة، ويمكن إضافة `note` يشرح التجهيز.

إجراء تحرير ملفين بالقلم قد ينشئ التزامين. الطريقة الموصى بها للأحداث المرتبطة ببيانات كتاب هي تنزيل الملفين من الأداة ورفعهما معًا إلى `data` في التزام واحد. تعليمات الاسترجاع في [حل المشكلات](TROUBLESHOOTING.md).
